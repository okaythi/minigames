# minigames

Minigames is a Vite + React + TypeScript starter for building small browser games.

## Scripts

- `npm run dev` starts the development server.
- `npm run build` runs a production type-check and build.
- `npm run preview` serves the production build locally.
- `npm run typecheck` runs the TypeScript project build without emitting files.